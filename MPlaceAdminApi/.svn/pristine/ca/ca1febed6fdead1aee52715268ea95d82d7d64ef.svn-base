package com.telemune.marketplace.rest.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.telemune.marketplace.rest.entity.HttpLinks;

public interface HttpLinksRepository extends JpaRepository<HttpLinks, Integer> {

	HttpLinks findByLinkId(Integer id);

}
